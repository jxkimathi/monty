#include "monty.h"

/**
 * nop_opcode - Implements the nop opcode
 * @top: Double pointer to the top of the stack
 * @line: Line number in the stack
*/

void nop_opcode(stack_t **top, unsigned int line)
{
	(void) line;
	(void) top;
}
